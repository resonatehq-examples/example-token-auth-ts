## What

An example of using Resonate's token-based authentication.

## Commands

- Use `npx tsx <file>` to run TypeScript files
- Use `npm install` to install dependencies
- Use `npm test` to run tests
